{
  "name": "json.path.do",
  "module": "worker.js",
  "version": "1.0.0",
  "description": "Dynamically Transform JSON Objects with JSONPath",
  "repository": "git@github.com:drivly/json.path.do.git",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "jsonpath-plus": "^7.1.0"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
